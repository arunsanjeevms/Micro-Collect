import 'package:flutter/material.dart';

/// MicroCollect Design System Colors
/// Extracted from Stitch "Rural Collection Pro" — Growth & Trust Theme
class AppColors {
  AppColors._();

  // ─── Primary ───────────────────────────────────────────────
  static const Color primary = Color(0xFF004532);
  static const Color primaryContainer = Color(0xFF065F46);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onPrimaryContainer = Color(0xFF8BD6B7);
  static const Color inversePrimary = Color(0xFF8BD6B6);
  static const Color primaryFixed = Color(0xFFA6F2D1);
  static const Color primaryFixedDim = Color(0xFF8BD6B6);

  // ─── Secondary ─────────────────────────────────────────────
  static const Color secondary = Color(0xFF855300);
  static const Color secondaryContainer = Color(0xFFFEA619);
  static const Color onSecondary = Color(0xFFFFFFFF);
  static const Color onSecondaryContainer = Color(0xFF684000);
  static const Color secondaryFixed = Color(0xFFFFDDB8);
  static const Color secondaryFixedDim = Color(0xFFFFB95F);

  // ─── Tertiary ──────────────────────────────────────────────
  static const Color tertiary = Color(0xFF393768);
  static const Color tertiaryContainer = Color(0xFF504E80);
  static const Color onTertiary = Color(0xFFFFFFFF);
  static const Color onTertiaryContainer = Color(0xFFC6C2FC);
  static const Color tertiaryFixed = Color(0xFFE3DFFF);
  static const Color tertiaryFixedDim = Color(0xFFC4C1FB);

  // ─── Surface / Background ─────────────────────────────────
  static const Color background = Color(0xFFF0FDF4);
  static const Color surface = Color(0xFFF0FDF4);
  static const Color surfaceDim = Color(0xFFD0DDD5);
  static const Color surfaceBright = Color(0xFFF0FDF4);
  static const Color surfaceContainerLowest = Color(0xFFFFFFFF);
  static const Color surfaceContainerLow = Color(0xFFEAF7EE);
  static const Color surfaceContainer = Color(0xFFE4F1E8);
  static const Color surfaceContainerHigh = Color(0xFFDEEBE3);
  static const Color surfaceContainerHighest = Color(0xFFD9E6DD);
  static const Color surfaceVariant = Color(0xFFD9E6DD);
  static const Color surfaceTint = Color(0xFF1B6B51);

  // ─── On Surface ───────────────────────────────────────────
  static const Color onBackground = Color(0xFF131E19);
  static const Color onSurface = Color(0xFF131E19);
  static const Color onSurfaceVariant = Color(0xFF3F4944);
  static const Color inverseSurface = Color(0xFF27332D);
  static const Color inverseOnSurface = Color(0xFFE7F4EB);

  // ─── Outline ──────────────────────────────────────────────
  static const Color outline = Color(0xFF6F7973);
  static const Color outlineVariant = Color(0xFFBEC9C2);

  // ─── Error ────────────────────────────────────────────────
  static const Color error = Color(0xFFBA1A1A);
  static const Color errorContainer = Color(0xFFFFDAD6);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color onErrorContainer = Color(0xFF93000A);

  // ─── Semantic Status ──────────────────────────────────────
  static const Color success = Color(0xFF059669);
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color danger = Color(0xFFE11D48);
  static const Color dangerLight = Color(0xFFFFE4E6);
  static const Color info = Color(0xFF2563EB);
  static const Color infoLight = Color(0xFFDBEAFE);

  // ─── Glassmorphism ────────────────────────────────────────
  static const Color surfaceGlass = Color(0xCCFFFFFF); // rgba(255,255,255,0.8)
  static const Color surfaceGlassBorder = Color(0x80FFFFFF); // rgba(255,255,255,0.5)

  // ─── Chart Colors ─────────────────────────────────────────
  static const Color chartGreen = Color(0xFF059669);
  static const Color chartAmber = Color(0xFFF59E0B);
  static const Color chartRed = Color(0xFFE11D48);
  static const Color chartBlue = Color(0xFF2563EB);
  static const Color chartIndigo = Color(0xFF6366F1);
  static const Color chartTeal = Color(0xFF0D9488);

  // ─── Card Border ──────────────────────────────────────────
  static const Color cardBorder = Color(0xFFE2E8F0);

  // ─── Misc ─────────────────────────────────────────────────
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);
}
